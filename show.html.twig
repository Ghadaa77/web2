{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}
    <h1>Produit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdProduit</th>
                <td>{{ produit.idProduit }}</td>
            </tr>
            <tr>
                <th>PrixProduit</th>
                <td>{{ produit.prixProduit }}</td>
            </tr>
            <tr>
                <th>TypePaiement</th>
                <td>{{ produit.typePaiement }}</td>
            </tr>
            <tr>
                <th>TypeProduit</th>
                <td>{{ produit.typeProduit }}</td>
            </tr>
            <tr>
                <th>DescriptionProduit</th>
                <td>{{ produit.descriptionProduit }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ produit.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_produit_front_index') }}">back to list</a>

    <a href="{{ path('app_produit_front_edit', {'idProduit': produit.idProduit}) }}">edit</a>

    {{ include('produit_front/_delete_form.html.twig') }}
{% endblock %}
