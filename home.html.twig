{% extends 'baseFront.html.twig' %}
{% block body %}

<h1>Liste des produits</h1>
    
        <div class="form-group">
            <input type="text" name="q" id="searchTerm" class="form-control" placeholder="">
        </div>
        <button type="submit" class="btn btn-primary">Rechercher</button>
    </form>
<br><br><br>

    <h1><center>Liste des produits</center></h1>
    <br><br><br>








        {% for produit in produits %}

<div class="card mb-3" >
  <div class="row no-gutters">
    <div class="col-md-4">
      <img src="{{ asset('images/' ~ produit.image) }}" style="width: 400px; height: 300px;" />
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h5 class="card-title">Type Produit: {{ produit.typeProduit }}</h5>
        <p class="card-text">Type Paiement: {{ produit.typePaiement }}</p>
        <p class="card-text">Prix Produit: {{ produit.prixProduit }} dt</p>
        <p class="card-text">Description Produit: {{ produit.descriptionProduit }}</p>
        
        <center>{{ include('produit_front/_delete_form.html.twig') }}</center>
         <a href="#">Acheter</a>
      </div>
    </div>
  </div>
</div>









        {% else %}
           
        {% endfor %}
        </tbody>
    </table>
</div>
   <center><a href="{{ path('app_produit_front_new') }}" >Ajouter un produit</a></center>
   
   <br><br><br>
{% endblock %}
