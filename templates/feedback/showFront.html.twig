{% extends 'baseFront.html.twig' %}

{% block title %}Feedback{% endblock %}

{% block body %}
    <h1>Feedback</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ feedback.id}}</td>
            </tr>
            <tr>
                <th>Favoris</th>
                <td>{{ feedback.favoris ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_feedback_indexFront') }}">back to list</a>

    <a href="{{ path('app_feedback_editFront', {'id': feedback.id}) }}">edit</a>

    {{ include('feedback/_delete_formFront.html.twig') }}
{% endblock %}
