{% extends 'baseBack.html.twig' %}

{% block title %}Feedbackp{% endblock %}

{% block body %}
    <h1>Feedbackp</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ feedbackp.id }}</td>
            </tr>
            <tr>
                <th>Favoris</th>
                <td>{{ feedbackp.favoris ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_feedbackp_index') }}">back to list</a>

    <a href="{{ path('app_feedbackp_edit', {'id': feedbackp.id}) }}">edit</a>

    {{ include('feedbackp/_delete_form.html.twig') }}
{% endblock %}
